import React from 'react';

const AuthServiceTest: React.FC = () => {
  return (
    <Card className="w-full bg-card">
      <CardHeader className="bg-card">
        <CardTitle className="bg-card">AuthService Unit Tests</CardTitle>
        <CardDescription className="bg-card">Unit tests for auth.service.ts</CardDescription>
      </CardHeader>
      <CardContent className="bg-card">
        {/* コンテンツをここに配置 */}
      </CardContent>
    </Card>
  );
};
export default AuthServiceTest;